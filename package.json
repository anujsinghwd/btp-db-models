{
  "name": "btp-db-model",
  "version": "1.0.0",
  "description": "Mongoose models and constants for BTP applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mongoose",
    "models",
    "btp",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "mongoose": "^6.0.0 || ^8.0.0"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "mongoose": "^8.0.0",
    "typescript": "^5.3.3"
  }
}
